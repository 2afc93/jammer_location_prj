1. To run the simulation just run "Main_isotropic" or "Main_anisotropic" in MATLAB.
2. To choose between the different filters just change the value of the variable "filter" writing the desired filter "EKF", "PF", "UKF", etc
3. The animation is disabled by default